Ao clonar o repositório deve-se modificar o token presente em src/config/graphql.js com um token válido e executar:

### `yarn start`
Executa o aplicativo em modo desenvolvimento em http://localhost:3000
